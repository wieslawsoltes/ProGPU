# Agent Guidelines & Reference Handbook (agents.md)

Welcome, agent! This document serves as a specialized developer guide and architectural handbook for AI coding agents pair-programming on the **ProGPU** codebase. Read this document carefully to align with our established design patterns, mathematical conventions, and diagnostic tools.

---

## 1. Core Architectural Rules & Conventions

### A0. Reflection-Free WPF Port Support
When adding ProGPU APIs for the WPF port, keep hot paths typed and source-integrated. Runtime reflection is allowed only for diagnostics, compatibility probes, or transitional adapters with a documented removal path; rendering, text, image upload, clipping, hit testing, shader effects, DirectX shims, cache metadata, and platform services should be implemented as reusable ProGPU/Silk.NET primitives or neutral DTO contracts instead of WPF bridge workarounds.

Cross-assembly WPF bridge contracts must not expose shim-owned WPF structs or classes when package-mode apps load the real WPF transport assemblies. Prefer primitive values, package-neutral DTOs, typed registrars, and source-integrated WPF interfaces such as the portable geometry, brush, pen, effect, bitmap-effect input, shader-effect sampler kind/image-source metadata, drawing-content, render-data, invalidation, visual-state, visual-bounds, visual-layout, and bitmap-source pixel seams.

### A. Rendering Quality & DPI-Aware Text Snapping
ProGPU achieves high-performance vector graphics and text rendering matching macOS Retina quality. When modifying or extending text visuals (`TextVisual.cs`, `Compositor.cs`, `GlyphAtlas.cs`):
* **Framebacks**: Always back the swapchain with physical framebuffer pixels (`FramebufferSize`) rather than logical window coordinates to prevent OS-level linear stretching.
* **Glyph Atlas scale**: Rasterize glyphs into the atlas at their actual physical dimensions by applying the current `DpiScale`.
* **4-Way Subpixel Snapping**: Snapped positions must be calculated in physical coordinates snapped to 1/4th of a physical pixel, then snap-backed (divided by `DpiScale`) for logical projection matrix orthographic drawing.

### B. Precise Winding Intersection Rules (Anti-Artifacting)
When modifying the GPGPU compute rasterizers
(`src/ProGPU.Backend/Shaders/GlyphRasterizer.wgsl` and
`src/ProGPU.Backend/Shaders/PathRasterizer.wgsl`):
* **Scanline Boundary Crossings**: To prevent horizontal line artifacts and drop-out seams at transition vertices (e.g. line-to-curve, curve-to-line, or curve-to-curve), the ray-casting algorithm must use **Precise Direction-Aware Half-Open Winding Intervals** based on the vertical derivative sign (`deriv_y`) at the intersection point:
  * **Upward Crossing (`deriv_y > 0.0`)**: Use the half-open interval `[0.0, 1.0)` (inclusive of start, exclusive of end).
  * **Downward Crossing (`deriv_y < 0.0`)**: Use the half-open interval `(0.0, 1.0]` (exclusive of start, inclusive of end).
* Apply this rule consistently across both `GlyphRasterizerShader` and `PathRasterizerShader` for all quadratic and cubic segments to avoid boundary vertex double-counting or zero-counting.

### C. WinUI Dynamic Theming & Styling
When creating or refactoring UI controls:
* **No Static Color Brushes**: Never instantiate solid brushes statically or assign them directly via static references in layout constructors (e.g., `new SolidColorBrush(...)` or `ThemeManager.GetBrush(...)`).
* **Dynamic Brushes**: Always bind brushes dynamically using `new ThemeResourceBrush("BrushKey")`. This registers the resource with the control's theme map (`_localThemeResources` / `_styleThemeResources`) so they automatically invalidate and re-resolve against the palette during theme switches (Light/Dark).
* **Property Invalidation**: Ensure visual elements invalidate both their measure/arrange states and visual caches on theme updates to force high-DPI assets and colors to repaint.

### D. Mandatory Shader Source and Complexity Contract

Static production shader source must be reusable, reviewable source code rather than a C# string literal.

* Put each fixed WGSL, GLSL, or HLSL module or composition template in its owning project's `Shaders/` directory, using the proper `.wgsl`, `.glsl`, or `.hlsl` extension. Keep one logical module or template per file.
* Load embedded shader source through `ProGPU.Backend.ShaderResource` into a `static readonly string`. The shared build glob in `Directory.Build.props` embeds the files, and the loader caches UTF-8 decoding by assembly and filename. Never open a manifest stream, read a shader file, concatenate fixed helpers, or decode source in a frame, draw, dispatch, atlas, or pipeline-cache hot path.
* Every shader file must begin with meaningful `// Algorithm:`, `// Time complexity:`, and `// Space complexity:` lines. Describe the actual algorithm, define symbols such as segment count or kernel radius, state average and worst-case behavior when they differ, and include material private-storage, output-storage, texture-sample, or bandwidth costs.
* Document fixed loop bounds, workgroup dimensions, numerical assumptions, alpha conventions, and quality-sensitive approximations next to the relevant shader code. Complexity documentation must be updated when an algorithm, sample footprint, loop bound, or storage layout changes.
* Runtime generation is allowed only when the output structurally depends on caller input, such as HLSL translation, dynamic WPF sampler declarations, or user ShaderToy code. Move every fixed prefix, wrapper, and helper used by that generator to a shader resource, cache it, and document the generated algorithm and cost model. Do not use generation as an exception for ordinary fixed shaders.
* Small test-only shader literals may remain inline when they are the direct input under test. Reusable fixtures and all production modules belong in shader files.
* Run `ShaderResourceTests` after adding or moving a shader. Its source audit rejects new inline production stage modules and verifies that every resource is embedded and carries the required algorithm and complexity contract.

---

## 2. Rendering Performance Regression Contract

Rendering performance and pixel quality are one contract. A change is not acceptable when it improves FPS by bypassing required invalidation, lowering raster quality, changing DPI/subpixel behavior, or silently dropping dynamic content.

### A. Preserve Compiled-Scene Correctness

When editing `Compositor`, `Visual`, atlases, effects, layers, or host frame code:

* Every mutation that can affect pixels must invalidate the owning visual and propagate `ChangeVersion` to the compiled-scene root. Never mutate retained command data behind the cache without invalidation.
* Layout-property setters must invalidate both their layout phase and visual pixels. Do not rely on a later `Size` or `Offset` assignment to advance the retained-scene version; a valid layout result can compare equal while clipping, padding, alignment, or descendant output changed.
* Keep cache validation sensitive to logical/physical target dimensions, viewport, DPI, glyph/path atlas generations, tooltips, external layers, and `CacheAsLayer` texture identity/dirty state.
* Increment atlas `Generation` whenever cached UV contents are cleared, moved, or repacked. Do not increment it for a no-op capacity probe.
* Do not make mutable `DrawingVisual`, masks, effects, or dynamic diagnostics cacheable until they have an explicit immutable version contract and regression tests.
* Keep `EnableCompiledSceneCache` and `EnableGpuHitTesting` independently configurable. WinUI uses CPU visual-tree hit testing and must not rebuild or even precompile paths for the duplicate GPU index when that option is disabled.
* A compiled-scene hit may skip CPU compilation and uploads, but it must still execute the render pass and current clear/presentation behavior.

Required focused tests live in `LayerRenderTests` and `CompositorReviewRegressionTests`. Cover at least unchanged reuse, visual invalidation, resize/DPI/target invalidation, mutable drawing visuals, atlas reset/repack, and hit-testing option behavior when changing these contracts.

### B. Protect Hot Paths

* Never unconditionally invalidate the sample root each frame. Invalidate only the visual whose content changed.
* Never request the next animation frame from inside `OnRender`. Advance animation and invalidate before compositor compilation from the host update/render callback, because render completion marks the node clean and can consume an in-render invalidation.
* Do not update animations twice. `Window.RenderFrameCore` owns the core `UpdateAnimations` call; sample callbacks should update only sample-specific state.
* Keep status/diagnostic text updates rate-limited. Rebuilding `RichTextBlock` inlines every frame defeats both its command cache and whole-scene reuse.
* Mutable inline content must notify its owning text control. Preserve `TextElement.Changed` subscription across the retained inline tree so `Run.Text`, font, size, and foreground changes invalidate without unrelated pointer input. Caret, selection, and hyperlink-hover changes are repaint-only and must use `InvalidateTextRendering`; do not invalidate measure or rebuild positioned characters for them.
* Preserve navigation-tree identity. Switching `NavigationView.Content` must not clear/re-add the pane, menu items, or unchanged `SplitView` children. Reparenting may recursively reapply theme state only when the resolved theme or theme family changes; same-theme cached page switches must not perform O(N) subtree theme invalidation.
* Keep virtualized indexed sources indexed. `ItemsControl` attachment to `IList` is O(1), and viewport realization is O(V) for V visible/overscan items. State-only refreshes must rebind active containers in place; do not recycle/reparent them. Do not eagerly enumerate/copy large glyph or data sources, allocate viewport scratch lists, create binding delegates during measure/arrange/scrolling, or remove/re-add overlay children merely to change z-order.
* Expensive reusable discovery belongs off page activation. Keep system-font enumeration and TextMate grammar loading shared and asynchronously warmed. Code editors must retain the immediate themed plain-text fallback and retokenize on the UI dispatcher when grammar resources become ready; do not create one registry/grammar per editor.
* Keep one immutable process-wide Markdig pipeline and warm it asynchronously before a Markdown page opens. `MarkdownTextBlock` must relayout when width or multi-column height changes, clear positioned text/children/commands when content becomes empty, and retain its generated commands until layout, theme, or hyperlink-hover state changes. Stable replay is `O(C)` for C commands with no parse/layout/string allocation; content changes remain `O(B + G)` for B parsed nodes and G positioned glyphs.
* Preserve retained text outlines in icon and glyph-browser hot paths. Record the font's cached raw outline and an official `DrawPath` transform; do not clone geometry per cell/frame or drop cubic/arc segments while transforming.
* MotionMark and similar vector benchmarks must use public `DrawingContext` APIs with retained `PathGeometry`. Drive attached samples through `IAnimatedElement`, rebuild changed split groups and invalidate before compilation, and keep `OnRender` scheduling- and geometry-mutation-free. Geometry generation may be O(N) when data or viewport changes; selecting due split toggles is O(K), rebuilding groups is O(N + G), and steady grouped recording is O(G) for N segments, K toggles, and G groups. Segment objects, brushes, pens, HUD resources, group-end indices, and pooled group containers must be reused with no per-segment managed allocation after warmup.
* Preserve `TextRunGlyph.GlyphIndex`; do not repeat character-map lookup during compositor compilation. Cache font table capability flags and hoist transform, raster-size, basis, and bold invariants out of glyph loops.
* Framework backends must record ordinary solid outline text as one retained `DrawGlyphRun` command. Preserve the shaped glyph-index array, convert shaped positions once when the platform glyph run is created, and reuse both arrays across invalidations. Never expand common text into one `DrawPath` command or one path-atlas entry per glyph. Recording must stay O(1) with no glyph-count-dependent allocation; compositor compilation remains O(G) for G glyphs. Gradient brushes and color/bitmap fonts may use their dedicated path or texture fallbacks.
* CFF and explicit vector-glyph fallback must preserve baked local fractional-position coverage because replacing it with one origin-relative coverage texture regresses Skia/Svg.Skia output. Canonicalize each local axis to the 128-phase lattice, carry the residual in the draw transform, use four device-translation phases per axis after the parent transform, and quantize scale only for vector text to ten binary mantissa bits. Ordinary paths retain 64 device phases per axis. Never cache an exact unbounded fractional position or suppress the parent-transform phase. Local coverage error is at most 1/256 coordinate per axis, vector-text device coverage error is at most 1/8 pixel, relative scale error is at most 1/2048, final quad placement remains unsnapped, and 8x8 high-precision winding remains unchanged. Compilation is O(G); one glyph/style/size has at most 128² local and 16 device-phase variants, the transformed-outline cache remains globally capped at 4,096 entries, and actual PathAtlas residency is bounded by atlas capacity. Keep the rotating 512-position regression at no more than 130 resident paths and the 256-position parent-transform regression at no more than 16 entries, with visible coverage for every glyph.
* PathAtlas frame advancement must preserve valid UV coordinates and `Generation`; render-target width/height are not path-demand estimates and must never trigger a speculative clear. After an actual recoverable capacity failure, abort CPU compilation, reset once, increment `Generation`, and retry the same onscreen or offscreen frame from retained commands before any moved UV is submitted. Do not defer recovery to a future frame because screenshots and one-shot surfaces have no such opportunity. A live set that still cannot fit after one reset must fail explicitly rather than loop or silently drop geometry. Add a stress test with fractional transforms and a deliberately small atlas whenever changing vector-text/path cache keys, scale or phase quantization, packing, cleanup, or fallback behavior.
* Empty rich text must clear positioned characters, table decorations, embedded children, and recorded commands transactionally during measure, including the pre-arrange state. Never return early for a missing font or empty inline collection while stale render output remains cached.
* Classify vector-text coverage once per text command, reusing the already computed transform scale. Physical size must include font size, transform scale, current display DPI, and static-buffer zoom. Keep the calibrated small/large device-pixel policy and transformed-text branch out of per-glyph loops.
* Preserve Skia font-transform separation. `GetGlyphPath`/`GetTextPath` materialize `x' = x * ScaleX + y * SkewX`, while retained fill glyph runs bypass those APIs and carry the same local transform on the command. Never feed an already transformed path through the command shear again. Neither representation may rescale explicit glyph-run positions or shaped advances a second time. Include local stretch and shear in vector glyph cache keys, and use the existing atlas vertex stretch/shear fields instead of allocating transformed position or outline arrays in the common fill-text path. Keep y-up TrueType and y-down SVG color-glyph shear signs explicit and covered by CPU-only tests.
* Preserve `SKPathBuilder` as a CPU-only retained-geometry adapter. Snapshot/add/transform/reverse operations are `O(N)` in path segments and detach is `O(1)`; do not initialize WebGPU, flatten analytic arcs, or add tessellation to builder operations. Keep endpoint, oval, round-rectangle, circle, and tangent arcs as `ArcSegment` values. Rational conics use exactly 16 midpoint-matched quadratic spans with sampled error at most `0.001`, keeping bounded `O(1)` work and storage per conic. Preserve native current-point behavior after close, rectangle and round-rectangle direction/start indices, true first-contour extend, reversed contour order/control points/arc sweep, and independent snapshot/detach ownership.
* Preserve legacy `SKPath` topology as a CPU view over retained geometry. Keep the intentional public `Geometry` bridge while matching the SkiaSharp 4.148 declared surface otherwise. Point/verb/mask/primitive/convexity queries and raw/force-close iterators must not initialize WebGPU or mutate render geometry. Coalesce consecutive compatible analytic arcs before emitting exact 90-degree rational conics; do not permanently flatten them. Keep the rational-conic marker attached across clone, transform, offset, extend, and reverse so 16 render spans still report one conic verb and weight. Iterator/query construction remains `O(N + Q)` time and storage, and `ConvertConicToQuads` remains the recursive Skia subdivision with `O(2^p)` work for power `p`. Preserve close-buffer semantics, synthetic close-line flags, move-only contour filtering, post-close relative origins, indexed primitive starts, and closed-contour extend.
* Preserve `SKRoundRect` as normalized CPU-only retained geometry with exactly four corners and an eagerly maintained native type classification. Keep Skia's double-precision CSS overlap scale, ULP side-fit correction, square-corner clamping, nine-patch detection, four-corner ellipse containment, and tangent/control-point reconstruction for axis-preserving transforms. Arbitrary rotation, skew, and perspective must fail instead of degrading into a loose bounding rounded rectangle. Setters, queries, validation, inset/outset, offset, and transform stay fixed `O(1)` and allocation-free; only the public `Radii` ownership boundary returns a four-point copy. Renderer code must use the internal retained radii and must not initialize WebGPU, tessellate, or allocate per draw.
* Preserve `SKPoint`, `SKSize`, `SKSizeI`, `SKRect`, and `SKRectI` as CPU-only `O(1)` value operations with no heap allocation or WebGPU initialization. Match native normalization, distance, reflection, arithmetic, checked size conversion, formatting, half-open point containment, exact `Empty` identity, degenerate inclusive intersection, unconditional min/max union, standardized coordinates, centered aspect fit/fill, and direction-aware inward/outward integer rounding. Point-based canvas overloads must remain thin delegates to the scalar implementations; do not fork command recording or rendering behavior.
* Preserve `SKColor` and `SKColorF` as immutable CPU-only `O(1)` values. Keep packed ARGB identity, named channels, parsing, copy-with-channel methods, equality, the `0.001` HSL/HSV chroma epsilon, clamping, nearest-byte packing, and the precomputed float `1 / 255` unpack scale aligned with native SkiaSharp. Do not expose mutable channel fields, allocate render state, or initialize WebGPU for color conversion.
* Preserve color-space transfer and XYZ math as CPU-only fixed-work algorithms. Named transfer parameters, HDR markers, signed sRGB-like branches, PQ/HLG equations, and the skcms bit-level log2/exp2 approximation must remain value-identical to native. Transfer and 3x3 inversion/concatenation are allocation-free `O(1)` operations; matrix storage stays inline, double-precision cofactors and row-major `A * B` order stay explicit, and only the public fixed-size `Values` ownership boundary may allocate. Do not initialize WebGPU for any color-space value query.
* Preserve `SKFont` encoded-text semantics. Validate an entire UTF-8/16/32 run before producing partial output, count supplementary scalars once, treat glyph-ID buffers as native-endian 16-bit values, and return consumed code units/bytes from `BreakText`. Keep span overloads allocation-free, array overloads single-allocation, caller tails untouched, and undersized outputs guarded instead of reproducing native wrapper overreads. Path fallbacks, intercepts, and `GetGlyphPaths` receive already transformed outlines. Text-on-path must cache repeated glyph paths and morph contour points; do not replace it with a single tangent transform per glyph.
* Preserve legacy `SKPaint` text as a thin adapter over its owned `SKFont` snapshot. Constructor, clone, reset, and `ToFont` must keep snapshots independent; `IsAntialias` and `LcdRenderText` jointly derive font edging. Route every UTF, glyph-ID, pointer, measurement, break, position, width, path, and intercept overload through the existing `SKFont`/`SKTextBlob` engines. Never add a second text decoder or glyph loop. State operations stay `O(1)`, encoded adapters stay `O(N)` with `O(1)` parsing workspace, returned arrays stay `O(G)`, and intercepts stay `O(G * S)` without initializing WebGPU.
* Preserve `SKTypeface` ownership and metadata semantics. Read glyph counts, PostScript names, fixed-pitch state, and SFNT table tags from the parsed font; keep table payloads borrowed internally and copy only at API ownership boundaries. Legacy glyph APIs must delegate to the validated `SKFont` encoding path. Empty typefaces must return empty glyph, metric, and table results rather than leaking the backing sentinel font. Do not expose variable-font clone or axis APIs until `fvar` positions are applied to both outlines and metrics.
* Preserve Skia canvas and read-stream state semantics. Public canvas save counts are one-based even though the internal stack is zero-based; `Save`/`SaveLayer` return the prior count, `RestoreToCount` clamps at one, and auto-restore is idempotent. `SKStream` remains an `SKObject`; managed callback streams must preserve ownership and dispatch after disposal must fail. Read directly into caller spans instead of renting or copying per read. Seekable skip stays `O(1)` with no read; forward-only skip stays `O(N)` with one pooled buffer capped at 80 KiB, never proportional storage. Unknown-length streams reach EOF only after a short or zero read. Peeks and snapshots preserve cursor position, missing file streams remain invalid without throwing, and memory-backed pointers stay pinned until disposal. Do not add per-read file I/O or WebGPU initialization to codec or rendering paths.
* Preserve Skia writable-stream encodings and ownership. Fixed-width values are little-endian; packed unsigned values use the one-byte/`0xfe`+16-bit/`0xff`+32-bit format; scalar text is invariant and uses `inf`, `-inf`, and `nan`. Keep primitive writes allocation-free, keep `WriteStream` linear with bounded pooled memory, and zero-fill a short source rather than leaking native uninitialized bytes. Managed writers leave the backing stream open unless ownership is explicit. Dynamic copy operations preserve buffered bytes; detach returns owned data or a seekable stream and resets the writer. Invalid file writers must reject writes without leaving a partial file.
* Preserve document export lifecycle and ownership. Empty or aborted PDF/XPS documents emit zero bytes; managed streams remain caller-owned, path overloads own their writer, close is idempotent, and content rectangles clip the page canvas. Capture each page at most once, and apply raster DPI to both target dimensions and the retained-command transform so logical geometry does not shrink. PDF metadata strings must be escaped before entering the object graph, cross-reference offsets must be computed from final byte positions, and XPS entries must retain stable package paths. Keep export `O(P + B)` time and storage for P captured pixels and B encoded bytes; metadata, options, abort, and empty-document paths remain `O(1)` and CPU-only.
* Preserve `SKData` ownership semantics. Managed data must stay pinned for the lifetime of every shared view, valid subsets must remain zero-copy and outlive their parent, exact-length stream factories must return null after a short read, and external release delegates must run exactly once after the final view is disposed. Keep `Data`, `Span`, `AsSpan`, size, and emptiness queries allocation-free; copy only at APIs whose names or ownership contracts require a copy.
* Preserve `SKBitmap`/`SKPixmap` storage semantics. Bitmap `ByteCount` and pixel spans include row padding between rows but not trailing padding after the final logical row; pixmap `BytesSize` remains tightly packed while its span is stride-aware. Unknown color types may carry non-empty metadata with a null pixel pointer. Failed installs must not replace existing storage, and installed release delegates must run exactly once even when the context is null.
* Keep pixmap operations CPU-only and stride-aware. Subsets intersect source bounds, retain their pixel owner, and remain zero-copy; erase clips partial rectangles and rejects disjoint ones. Pixel reads return unpremultiplied colors, read/scale paths convert destination color and alpha representation, and PNG/JPEG encode must never upload a bitmap or initialize WebGPU. Preserve explicit false/null results for unavailable encoders rather than emitting a mislabeled format. Keep views and pixel queries `O(1)`, read/erase `O(P)`, scaling at the selected kernel complexity, and encoding `O(P + B)` time/storage.
* Preserve transactional bitmap copies and shared subsets. Convert into temporary owned storage and swap only after full success, leaving an existing destination untouched on failure. Subsets retain the original pixel owner and row stride; alpha extraction owns a one-byte mask with four-byte-aligned rows and zero offset. Pixel arrays are row-major and require exactly width times height entries. These paths stay CPU-only: subsets are `O(1)`, validation failures are `O(1)`, and copies, arrays, and alpha extraction are `O(P)`.
* Preserve bitmap shader sampling end to end. Every `ToShader` overload must retain tile modes and local matrix, and sampling must flow from `SKSamplingOptions` through `ImageShaderData` into each tiled `RenderCommand`. Nearest must not become linear; mipmap intent must remain explicit; custom cubic B/C coefficients must reach the texture shader unchanged. Upload the bitmap once per shader, not once per tile. Creation is `O(P)` for P pixels and tiled command recording is `O(T)` for T visible tiles.
* Preserve `SKImage` as a GPU-backed `SKObject` and keep its `GpuTexture`, backend constructors, and texture-wrapping helpers internal to the shim. Metadata, unique IDs, and context validity remain `O(1)`. Shader creation may snapshot the source texture once, but never once per tile; retain sampling, cubic coefficients, tile modes, and local matrices. `ToRawShader` must bypass color-space conversion while ordinary image shaders retain it. Readback is explicitly `O(P + D)` for P source pixels and D converted destination pixels, scaling adds the selected kernel cost, invalid destinations return `false` without partial writes, and GPU-backed `PeekPixels` must not expose an unstable CPU pointer.
* Preserve bitmap decode ownership and alpha semantics. Every file, byte/span, stream, data, and codec overload must converge on the same cached CPU decode result. Invalid stream/data/file factories return null, bounds queries return `SKImageInfo.Empty`, and direct byte/span wrappers retain native SkiaSharp's `ArgumentNullException` after failed codec creation. Default decode changes codec `Unpremul` metadata to a premultiplied bitmap and must not parse the same encoded payload twice or initialize WebGPU.
* Preserve `SKCodec` result, scaling, and state semantics. Missing files return `InternalError`, unknown encodings return `Unimplemented`, and recognized truncated inputs return `IncompleteInput`. Static images report zero animation frames; subset and scanline decode remain explicitly `Unimplemented`. Non-JPEG codecs reject scaled output, while JPEG dimensions use the nearest native eighth-resolution step. Keep the common full-size RGBA/unpremultiplied decode as a direct stride-aware row copy from cached pixels, keep incremental state `O(1)`, and never initialize WebGPU from codec factories, metadata, pixel decode, or failure paths. Factory acquisition is `O(B)` and decode/copy is `O(B + P)` for B encoded bytes and P output pixels.
* Preserve `SKImageInfo` as a pure value contract. Keep platform channel shifts aligned with `PlatformColorType`, keep every `SKColorType` numeric value and byte/bit width aligned with native SkiaSharp, report unknown or invalid types as zero bytes per pixel, use checked arithmetic for 32-bit row/image sizes, retain non-overflowing 64-bit variants, and include color-space identity in equality. Metadata queries must never allocate pixel buffers or initialize WebGPU.
* Preserve `SKRectI` as Skia's exact allocation-free value contract. Point containment and ordinary intersection are half-open, inclusive intersection retains boundary-touch rectangles, and `IsEmpty` means equality with `SKRectI.Empty`, not generic zero area. Keep reversed-extent standardization and inward/outward rounding direction-sensitive, keep floating-point aspect fit/fill centered before flooring edges, and keep overflow checked. Every rectangle operation remains `O(1)` and CPU-only.
* Preserve `SKRect` as Skia's exact allocation-free floating-point value contract. Point containment and ordinary intersection are half-open, inclusive intersection retains boundary-touch rectangles, and `IsEmpty` means equality with `SKRect.Empty`, not generic zero area. Union is the unconditional coordinate envelope and therefore includes the empty rectangle's origin; bounds accumulators must track their first value separately. Keep reversed extents and zero-size aspect resizing native-compatible. Every rectangle operation remains `O(1)` and CPU-only.
* Preserve `SKRoundRect` as Skia's fixed four-corner geometry contract. Keep UL/UR/LR/LL order, native type classification, square-corner clamping, CSS/W3C proportional radius scaling, double-precision side adjustment, ellipse containment, centered collapse on excessive inset, and axis-preserving transform rejection. Transform radii must be derived from mapped absolute anchors and ellipse centers so translation and corner subtraction retain native float-ULP behavior. Public `Radii` returns an independent four-point snapshot; path/canvas hot paths use the internal read-only span. All operations remain fixed-work `O(1)`, CPU-only, and allocation-free except the documented public snapshot and returned transformed object.
* Preserve `SKPoint` and `SKPointI` as Skia's allocation-free point contracts. Keep normalization, distance, arithmetic, offsets, vector conversion, equality, hashing, and formatting `O(1)` and CPU-only. Their native `Reflect` compatibility formula uses the point's squared length rather than a point-normal dot product; do not silently replace it with the conventional vector-reflection formula. Zero-vector normalization retains native NaN or integer-conversion behavior.
* Preserve `SKSize` and `SKSizeI` as Skia's allocation-free size contracts. Constructors and conversions must retain their native directionality; float-to-integer narrowing truncates under checked overflow semantics, including NaN and infinity rejection. Arithmetic, point conversion, equality, hashing, emptiness, and formatting remain `O(1)` and CPU-only.
* Preserve `SKMatrix` as Skia's full row-major 3x3 projective contract. Keep concat order exact, use double-precision inversion cofactors with finite validation, preserve the specialized tiny-scale path, map perspective vectors relative to the mapped origin, and map zero homogeneous `w` to zero. Perspective rectangle bounds must clip against `w >= 1/16384` in fixed stack storage before projection; do not replace this with unbounded corner division or flatten perspective through `Matrix4x4`. Scalar and rectangle operations remain allocation-free `O(1)`; span mapping is `O(N)` with `O(1)` auxiliary storage, and only result-array APIs may allocate `O(N)`.
* Preserve `SKRotationScaleMatrix` as SkiaSharp's mutable four-float RSXform contract. Keep identity, translation, uniform scale, radian/degree rotation, anchored creation, `ToMatrix`, float equality, and sequential hashing native-compatible down to single-precision evaluation order. Each value operation remains allocation-free fixed-work `O(1)` CPU work; glyph arrays may add only `O(G)` caller-owned storage for G glyphs.
* Preserve `SKMatrix44` as Skia's contiguous 16-scalar value contract. Keep `SKMatrix` translation/perspective placement, row and column major conversion, index validation, `System.Numerics.Matrix4x4` determinant/inversion/transpose/mapping semantics, and native pre/post concat order exact. Singular inversion returns `Empty`. Scalar, factory, mapping, algebra, inversion, and exact-length span-copy paths remain allocation-free fixed-work `O(1)` operations; only array-returning major-order APIs may allocate one 16-float result. Do not expose shim-only public conversion helpers.
* Preserve `SKColor` and `SKColorF` as Skia's allocation-free color contracts. Integer storage and uint conversion use packed AARRGGBB order. Float-to-byte conversion clamps to `[0, 1]`, maps NaN and negative values to zero, maps positive infinity to 255, and rounds with `value * 255 + 0.5`; byte-to-float conversion divides by 255. Keep the native 0.001 HSL/HSV chroma epsilon, hue wrapping, achromatic branches, and byte-returning factory truncation. Parsing, packing, conversion, channel replacement, equality, hashing, and color arithmetic remain `O(1)` and CPU-only.
* Preserve `SKColors` as SkiaSharp's one-byte palette struct. Named CSS colors remain mutable static fields with exact packed values, `Transparent` remains transparent white, and `Empty` remains a computed zero property rather than a field. Palette access is allocation-free `O(1)` CPU metadata.
* Preserve `SKColorSpaceXyz` and `SKColorSpaceTransferFn` as scalar CPU value contracts. Keep named D50 matrices and seven-parameter transfer definitions byte-compatible with native SkiaSharp. Matrix concat is row-major; inversion uses double-precision cofactors and returns `Empty` for singular or non-finite results. Transfer evaluation must retain signed inputs, `skcms` PQ/HLG markers, bounded fast log/exp behavior, NaN/infinity results, continuity checks, and inverse rejection semantics. Constructors, indexing, concat, inversion, evaluation, equality, and hashing remain allocation-free `O(1)` operations; only `Values` may allocate its fixed-size ownership snapshot.
* Preserve `SKColorSpace` and `SKColorSpaceIccProfile` as CPU-only construction and metadata contracts. Keep sRGB/linear singletons, native near-sRGB/gamma-2.2/linear snapping, gamma and numerical-function classification, CICP enum values, exact D50 matrices and transfer tables, PQ/HLG out-parameter behavior, and null/exception semantics. ICC parsing must validate every declared bound before slicing, copy caller bytes once into stable profile-owned storage, reject non-RGB profiles as color spaces while retaining valid profile metadata, and compare all three channel TRCs. Parametric curves stay `O(1)`; sampled tables are accepted only when all channels are close enough to sRGB and may perform one bounded `O(S)` comparison with `O(1)` workspace at profile construction, never during a frame, draw, texture conversion, or color-table lookup. Profile parsing remains `O(T + B)` time and `O(B)` owned storage for T tags and B bytes; all color-space queries remain `O(1)` and must not initialize WebGPU.
* Preserve `SKFontStyle` as SkiaSharp's owned metadata object. Keep default/enum/integer constructors unrestricted, retain exact weight/width/slant values, expose Normal/Bold/Italic/BoldItalic as stable static properties, and protect those singleton handles from public disposal while ordinary instances release theirs. Construction and queries remain allocation-bounded fixed-work `O(1)` CPU operations and must not parse fonts or initialize WebGPU.
* Preserve `SKSurfaceProperties` as SkiaSharp's owned CPU metadata object. Keep pixel-only, raw-uint, and typed-flag constructors; preserve unknown 32-bit flag and pixel-geometry values; derive device-independent-font use from bit 0; and release ordinary handles through inherited lifetime. Construction and queries remain allocation-bounded `O(1)` and must not initialize WebGPU.
* Preserve `SKPath` metadata and iteration against the retained figure/segment model. Count moves and closes as verbs but not close points; expose line, quad, conic, and cubic control/end points in native order; retain the last explicit endpoint after close; and recover rectangle direction, oval bounds, rounded-rectangle radii, and linear-contour convexity without rendering. Keep elliptical arcs analytic in rendering storage and split them only for metadata/iteration into rational conics of at most 90 degrees, with tolerance at exact quadrant boundaries. Nested raw/cooked iterators require exactly four point slots, leave unused slots untouched, preserve conic weights, and insert only the native cooked close line/verb. Construction may snapshot `O(V)` compact operations for V verbs; `Next`, `Peek`, and state queries remain `O(1)`. Other scans remain `O(S)` time and `O(1)` auxiliary storage for S segments; only caller-owned point/result APIs may allocate `O(P)`. These APIs must never flatten arcs to lines, initialize WebGPU, submit work, or read pixels.
* Preserve native `SKPath` current-point mutation semantics. Empty relative commands start at the origin; every control/end delta in one verb uses the same current point; commands after close use the contour origin. Offset and in-place transform retain the active figure and conic identity so later commands append to that contour. Destination transforms replace existing target geometry without mutating the source, preserve fill/conic state, and transform an aliased source/target exactly once. Relative commands remain allocation-free `O(1)`, in-place transforms `O(S)`, and destination transforms `O(S)` time/storage for S segments. `Rewind` clears geometry and resets winding fill.
* Preserve Skia conic subdivision and SVG path serialization. `ConvertConicToQuads` emits `2^P` quadratics and `2^(P+1)+1` points, recursively normalizes each child conic, and updates its weight to `sqrt((1+w)/2)`; do not substitute raw homogeneous midpoint recursion. Validate negative/excessive exponents and destination capacity before writing. `ToSvgPathData` uses the cooked iterator, invariant round-trip float formatting, exactly 32 ordinary quadratic commands per rational conic, and the explicit cooked closing line before `Z`; never serialize a weighted conic as one unweighted quadratic. Conversion is `O(2^P)` time/output with `O(P)` stack depth and serialization is `O(V)` time/output for V verbs; neither may initialize WebGPU.
* Preserve analytic Skia path construction. Degree oval arcs retain signed partial sweeps; `AddArc` makes a full sweep a closed two-half oval, while rectangle `ArcTo` makes a full sweep connector-only. Tangent arcs use `radius / tan(angle/2)` and fall back to one line for zero radius or degenerate vectors. Consecutive empty moves collapse. Indexed rectangles use literal starts 0..3; indexed round rectangles use literal starts 0..7, rotate line/arc edges, and omit a final line supplied by close, while the legacy counterclockwise overload keeps its direction-dependent start. Reverse append reverses contour order and segment order, swaps cubic controls, preserves conic weights, and flips arc sweep. Construction remains bounded `O(1)` except reverse append's `O(S)` time/storage, and must not flatten curves or initialize WebGPU.
* Preserve `SKPathBuilder` ownership and `SKPathMeasure` query performance. Builder mutations must retain analytic curves and conic weights; snapshot copies `O(S)` segments, detach transfers in `O(1)` and installs an empty winding path, and reset restores winding fill. Measure raw verbs once into cumulative tables using `Q = min(64, 1 + ceil(4 * sqrt(resScale)))` samples per curve; do not restore linear `resScale` oversampling or flatten a path for every query. Setup remains `O(S * Q)` time/storage and distance lookup `O(log(S * Q))`. Evaluate positions/tangents analytically at the recovered parameter, retain curve verbs in extracted segments, use De Casteljau slicing for ordinary curves and normalized homogeneous slicing for conics, and preserve native failed-output, clamping, zero-span, forced-close, and destination-replacement semantics. These APIs are CPU-only and must never initialize WebGPU.
* Preserve the two-tier `SKPath.Simplify`/`ToWinding` solver. Closed line contours use the CPU planar arrangement: pairwise split intersections, classify fragment fill on both sides, orient fill right, hash-deduplicate directed edges, and stitch adjacency-indexed loops. For E edges, I intersections, and F fragments, keep `O(E^2 + E*F)` time and `O(E + I)` storage; never restore repeated full-list fragment scans. Curves may use the exact GPU union solver only when a context is already active. With no context, preserve analytic curves and conic weights and normalize only fill type; never flatten or initialize WebGPU. Result overloads remain null-safe and alias-safe.
* Preserve `SKRegion` as canonical disjoint horizontal bands. Every rect/region boolean operation and bulk assignment must normalize y bands and merged x intervals before exposing bounds or iterators. Path assignment must flatten raw contours once, retain rational conic weights, and run allocation-free pixel-center winding/even-odd tests inside the clip; never call `SKPath.Contains` per pixel or initialize WebGPU. For R rectangles, normalization is worst-case `O(R^2 log R)` time/`O(R)` storage; for L flattened edges and A pixels, scan conversion is `O(L + A*L)` time/`O(L + R)` storage. Nested rect/clip iterators snapshot full canonical bands, clip iterators cull rather than crop, and span iterators emit merged exclusive runs.
* Preserve `SKDrawable` hook dispatch and canvas isolation. Bounds/memory queries call their virtual hooks; draw overloads save, compose the local matrix, invoke `OnDraw`, and restore even on failure; generation invalidation is atomic; and default snapshot records the drawable into a retained picture. Metadata remains `O(1)` and draw/snapshot remain `O(C)` for C commands without GPU submission or readback.
* Preserve retained `SKPicture` metadata and recorder ownership. IDs must be nonzero and process-unique; direct operation counts are `O(1)`, while recursive counts replace each nested-picture command with its child count in `O(C + N)`. Byte estimates walk owned command/buffer arrays without playback. Ending recording clears `RecordingCanvas`, transfers retained leases exactly once, and drawable completion owns an independent picture whose bounds and snapshots remain stable. Every picture-shader overload must retain tile modes, tile bounds, local matrix, and nearest/linear filter mode through the final texture command; never force all picture shaders to nearest sampling. Metadata and recording must not initialize WebGPU or read pixels.
* Preserve the versioned ProGPU picture archive as a portable, bounded command graph rather than a process-local registry token. Round-trip scalar command state, pooled buffers, analytic paths, every built-in vector brush, pens/dashes, font bytes, texture-patch metadata, meshes, glyph arrays, and nested pictures in `O(B + C)` time/storage. Validate the magic/version, total archive size, every count/string, enum, and nesting depth before allocating. Reject GPU textures, static buffers, cache-key objects, and custom extension payloads explicitly until they have a real portable codec; never serialize an address, silently omit a resource, trigger GPU readback, or claim native Skia `.skp` interchange compatibility.
* Preserve `SKPointMode` and `SKCanvas.DrawPoint(s)` through the existing stroke path. Ignore paint fill style but retain width/cap/color/blend/antialias/transform semantics; points are zero-length segments, lines consume pairs and ignore an unmatched tail, and polygons connect adjacent points without closing. Batch one input array into one `O(N)` retained path and one draw command; never emit one GPU command per point.
* Preserve native canvas transform composition and no-op rules. Scalar, point, pivot, degree, radian, skew, 3x3, and 4x4 overloads post-concatenate in Skia order; empty point transforms, identity scales, zero skews, and complete degree turns must avoid matrix churn. Transform calls remain allocation-free `O(1)` canvas-state updates and must not record commands, initialize WebGPU, submit work, or read pixels.
* Keep canvas point/size shape overloads as allocation-free `O(1)` adapters over the established retained line, rounded-rectangle, ellipse, and circle paths. Overload normalization must preserve coordinates, radii, paint semantics, one-draw command counts, and tessellation quality; never duplicate renderer implementations per overload.
* Preserve `SKCanvas.DrawPicture` as one nested retained-picture command for optional-paint, scalar-position, point-position, and local-matrix overloads. Compose local transforms after the current canvas matrix, restore canvas state in `finally`, and wrap the nested command with paint blend/opacity scopes without flattening. Setup remains allocation-free `O(1)` outside existing color-conversion caches, and playback remains `O(C)` for C nested commands with no readback.
* Route every `SKCanvas.DrawImage` point, scalar, destination, and source/destination overload through one sampling-aware texture path. Convert legacy `SKPaint.FilterQuality` exactly once to nearest, linear, linear-mipmap, or Mitchell sampling; preserve explicit cubic and anisotropic options. Overload setup stays allocation-free `O(1)`, records one texture draw plus required scopes, and must not read pixels.
* Preserve `SKCanvas.DrawImageLattice` and nine-patch rendering as one retained texture command and one indexed GPU texture draw per call. Follow Skia's alternating fixed/scalable segment rules exactly: preserve fixed source lengths while space permits, proportionally stretch scalable segments, and collapse scalable segments before shrinking fixed segments for undersized destinations. Leading divs equal to the lattice bound remove the degenerate first row or column and must advance `RectTypes`/`Colors` indexing. Omit transparent and zero-area cells; emit fixed-color cells through the texture batch without sampling, with correct straight/premultiplied alpha and paint color filtering. Layout is `O(X + Y + C)` time and `O(X + Y + C)` retained storage for X/Y divs and C visible cells; compositor expansion is `O(C)` with one draw-call boundary. Never record one command or issue one GPU draw per cell.
* Preserve every `SKCanvas.DrawAtlas` overload as one retained transformed-patch array and one indexed texture draw. Apply each `SKRotationScaleMatrix` to sprite-local coordinates before the canvas matrix, keep optional cull rectangles conservative rather than clipping sprites, and retain explicit nearest, linear, mipmapped, cubic, and anisotropic sampling. When colors are present, treat the sampled sprite as blend source and the per-sprite color as destination for every Skia blend mode; preserve straight/premultiplied texture output, paint opacity, color filters, masks, and framebuffer blending. Layout, bounds, and compositor expansion are `O(S)` time and storage for S visible sprites; never emit one command, texture copy, bind group, or GPU draw per sprite.
* Preserve `SKVertices` as an immutable copied mesh and every `SKCanvas.DrawVertices` overload as an adapter to one retained `DrawVertexMesh` command. Triangles, triangle strips, and triangle fans must normalize to one contiguous indexed vector batch; invalid indices are skipped without leaving unwritten index ranges. Keep per-vertex colors premultiplied in transport, evaluate all Skia vertex-color blend modes in the vector shader, retain texture coordinates for shader-space evaluation, and include each valid triangle in GPU hit testing. Construction is `O(V + I)` time and storage for V vertices and I indices; compilation is `O(V + T)` for T triangles and must not allocate one command, brush, path, or draw call per triangle.
* Preserve `SKCanvas.DrawPatch` as adaptive Coons-patch tessellation into one retained vertex mesh. Read the four clockwise cubics as top `0..3`, right `3..6`, bottom `9..6`, and left `0,11,10,9`; combine both ruled surfaces minus the bilinear corner surface. Derive device-space LOD from transformed boundary control-polygon lengths with Skia's 10-pixel partition, minimum 8x8 grid, and proportional cap at 60,000 indices. Bilinearly interpolate corner colors in premultiplied space and texture coordinates in local space. Tessellation costs `O(Lx * Ly)` time and storage and must transfer freshly allocated arrays once, retain one command, and compile into one vector batch rather than per-cell paths or draws.
* Keep Skia canvas clip metadata in fixed `O(1)` state synchronized with clip pushes and save/restore. Device bounds use nearest-pixel rounding for axis-aligned rectangles and outward rounding for arbitrary geometry; local bounds outset one device pixel before inverse mapping. Preserve empty/rectangular classification and rectangle-edge difference simplification. `LocalClipBounds`, `DeviceClipBounds`, `IsClipEmpty`, `IsClipRect`, `Get*ClipBounds`, and `QuickReject` must never scan retained commands, tessellate paths, submit GPU work, or allocate per query.
* Keep Skia canvas annotation data overloads as allocation-free raster no-ops: annotations do not alter pixel output and must not record render commands or initialize WebGPU. String URL/destination overloads return independent caller-owned null-terminated ASCII `SKData` in `O(N)` time/storage. Add document serialization behind document-specific metadata rather than injecting annotation work into ordinary raster scene compilation.
* Preserve `SKCanvasSaveLayerRec` as an opt-in extension of the existing offscreen layer path. Ordinary `SaveLayer` must not snapshot parent commands. `InitializeWithPrevious` and backdrop filters copy pending parent command/buffer state once, seed from already-flushed owning-surface pixels, replay active clips/bounds once, and release all cloned resource leases on restore/failure/disposal. Backdrop filtering precedes child content; `F16ColorType` uses an RGBA16F target and format-matched compositor. Snapshot setup is `O(C + B)` for C commands and B pooled values and must stay absent from the ordinary path; never read pixels back to CPU or submit once per parent command.
* Preserve native Skia canvas ownership semantics. Public `SKCanvas.Surface` and `Context` are allocation-free identity reads of the attached `SKSurface` and caller-supplied `GRRecordingContext`; raster/bitmap/picture canvases return null as native Skia does. Keep the retained ProGPU command recorder internal under the distinct `DrawingContext` name. `GRContext` must remain derived from `GRRecordingContext`, report the Dawn backend, and never create or dispose a second `WgpuContext` for metadata queries.
* Implement `SKCanvas.DrawBitmap` overloads only as SkiaSharp-compatible short-lived `SKImage.FromBitmap` adapters into `DrawImage`. Preserve legacy obsolescence and filter-quality mapping, explicit sampling, source/destination geometry, and one retained texture command; do not fork bitmap rendering or add readback.
* Route `SKCanvas.DrawSurface` and sampling-aware `SKSurface.Draw` overloads through one surface snapshot and the shared `DrawImage` path. Point forms only normalize coordinates; preserve sampling, paint, color management, and one texture command. Never add a second snapshot, readback, or surface-specific renderer.
* Preserve Skia fake-bold geometry. `SKFont.Embolden` uses a `Size / 32` mitered stroke-and-fill path, with generated stroke contours normalized to the dominant outer glyph winding so overlap cannot cancel the original interior. Positive fractional `SKPaint.GetFillPath` stroke widths must remain fractional; a zero-width device hairline has no device-independent fill path. Keep emboldened text on its widened-path quality path and ordinary text on the glyph-run fast path.
* Preserve the unified CPU `SKPaint.GetFillPath` stroker and its overload contracts. Cull rectangles are hints, never clips; matrix overloads derive resolution from the maximum affine scale; invalid or sub-unit scales normalize to one. Keep curve subdivision at `Q = clamp(ceil(24 * sqrt(max(1, resScale))), 24, 192)`, so conversion remains bounded `O(S * Q)` time/storage instead of scaling linearly with arbitrary transforms. Fill-only output preserves source fill type, stroke output uses winding, successful calls replace destinations transactionally, failed legacy path calls preserve them, failed builder calls retain a source snapshot, and source/destination aliasing must remain safe. Positive-width strokes succeed even when empty input or butt-capped degeneracy yields an empty result, while hairline return overloads return null. Never initialize WebGPU for path widening.
* Preserve `SKPathEffect` as an immutable paint-retained `SKObject` graph. Snapshot intervals and stamp paths at factory time; compose outer after inner and sum both original-path results. Keep a plain dash on the analytic pen fast path instead of expanding it per draw. Materialize non-dash effects once before retained recording, apply line-lattice stroke width and path-lattice fill mode to the cloned paint, and never leave the original effect attached to the generated path. Measured effects stay `O(S + G)`, corners `O(S)`, discrete paths `O(L / d)`, line lattices `O(R * E log E)`, and stamp lattices `O(C * V)` with a hard one-million-element bound and `O(E)` temporary scan storage. Reject invalid, perspective, overflowed, or excessively dense lattices before recording partial geometry. Do not initialize WebGPU, read pixels, or rematerialize an unchanged effect inside compositor loops.
* Preserve `SKBlender` as immutable `O(1)` paint state. Validate blend-mode factories, return null for non-finite arithmetic coefficients, keep explicit `SrcOver` paint assignment on the null fast path, share custom blender identity through clone, and clear it on reset/disposal. Every built-in mode must map to an exact `GpuBlendMode`; `Modulate` uses fixed-function `C = Cs * Cd`, `A = As * Ad` with no destination copy. Until arithmetic destination sampling is implemented, reject it before recording any command; never silently map custom coefficients to `SrcOver`. A future arithmetic path must evaluate `clamp(k1 * S * D + k2 * S + k3 * D + k4)` per component, enforce premultiplication only when requested, use one bounded offscreen composition boundary, and place its WGSL in a documented embedded shader resource.
* Preserve `SKShader` as an immutable retained `SKObject` graph with the complete SkiaSharp 4.148 factory surface. Snapshot gradient colors and positions once; never retain mutable caller arrays or reconvert stops per draw. `WithLocalMatrix` and `WithColorFilter` must return new nodes, retain their source, compose local matrices parent-before-child, and apply filters child-first. Keep ordinary colors, gradients, noise, images, pictures, and source-over conical composition on direct paths. For general mode or arithmetic composition, evaluate both shaders over device coverage, blend/filter before geometry coverage, then apply the target clip once; never blend two antialiased, already-clipped children. Reuse the embedded image-blend/arithmetic pipelines, preserve destination/source ordering, size intermediates from their inputs, retain no CPU readback, and keep up to four filter nodes allocation-free during traversal. Sweep shaders must retain start/end angles through brush cloning, GPU registration, shader evaluation, and portable picture serialization; update the documented `O(1)` WGSL angle algorithm whenever its mapping or degeneracy policy changes.
* Preserve `SKColorFilter` as an immutable retained `SKObject` graph with the complete SkiaSharp 4.148 factory surface. Require exact 20-value finite matrices and exact 256-entry tables, snapshot caller spans once, canonicalize blend no-ops, protect gamma singletons from public disposal, compose outer after inner, and lerp premultiplied child outputs. Luma-to-alpha must emit black RGB and include source alpha. High contrast must operate in linear light with Skia's grayscale/inversion order and machine-epsilon contrast pin; HSLA matrices must round-trip through unpremultiplied HSL. Keep blend, RGBA matrix, luma, gamma, and table leaves on existing direct/single-pass paths. Nonlinear leaves may use only the lazy cached `ProGPU.Compute/Shaders/NonlinearColorFilter.wgsl` resource, while compose/lerp reuse existing passes, release intermediates by graph depth, and never read pixels back. Construction stays `O(1)` except bounded matrix/table snapshots; evaluation stays `O(F * P)` texel work and `O(D * P)` live temporary storage for F leaves, P pixels, and graph depth D. Every new color-filter kind requires CPU semantics, API-shape coverage, resource/source guards, and a proof that common Svg.Skia filters did not gain an extra pass or eager pipeline compilation.
* Preserve `SKImageFilter` as an immutable retained DAG and `SKObject`, not eager GPU work. Keep the complete 4.148 overload surface on shared node constructors, Decal as default blur tiling, Mitchell as default image sampling, null inputs as source-graphic fallbacks, exact crop metadata, and finite custom `SKBlender` state. Copy convolution kernels and merge spans once; construction stays `O(1)` per ordinary node, `O(K)` for K coefficients, and `O(N)` for N merge inputs. Evaluation must memoize by source texture, node, and color-space policy so compose can rebind outer source leaves without stale cache hits while retaining `O(V + E)` scheduling per source context. Matrix filters must conjugate local transforms through the layer transform and keep native sampling. Magnifier must crop and fit its child input before lens evaluation, restrict output to the lens/child intersection, lazily compile `ProGPU.Compute/Shaders/Magnifier.wgsl`, preserve Skia's squared inset-distance and circular `2 * inset` corner weighting, and keep nearest/bilinear/bicubic work bounded to 1/4/16 reads per texel with no readback. Never initialize WebGPU from a factory. Validate required children, color filters, displacement inputs, images, pictures, blenders, tile inputs, exact kernel lengths, and finite non-empty magnifier parameters before retaining a node.
* Preserve `SKTextBlob` intercept semantics. Return at most one pair per intersecting positioned glyph in run order, do not merge overlapping glyph pairs, skip complete RSXform runs, apply font scale/skew/embolden and y placement, keep band point tests strict, and let span overloads truncate without touching the remaining destination. Quadratic and cubic band crossings must use bounded analytic roots with constant workspace; intercept queries must not initialize WebGPU or invoke GPU path operations.
* Preserve `SKRotationScaleMatrix` as an allocation-free `O(1)` CPU value transform. Identity, translation, scale, radian/degree rotation, anchored composition, and `ToMatrix` must match SkiaSharp 4.148. Keep degrees multiplied by the pre-divided float radians-per-degree constant, evaluate sine/cosine in double precision before casting to float, and preserve anchor-compensation expression order; RSXform placement is bit-sensitive. No factory may initialize WebGPU.
* Preserve `SKSurfaceProperties` metadata and lifetime semantics. `SKPixelGeometry` values are exactly `Unknown=0`, `RgbHorizontal=1`, `BgrHorizontal=2`, `RgbVertical=3`, and `BgrVertical=4`; do not restore obsolete members or shift the values. Keep `SKSurfacePropsFlags` bit-compatible, retain unknown `uint` bits, derive device-independent-font state from bit 0, and leave disposal inherited from `SKObject`. Construction and queries are `O(1)` and must not initialize WebGPU.
* Preserve `SKFontStyle` ownership and singleton semantics. Keep the default constructor, immutable integer weight/width/slant values, and static `Normal`/`Bold`/`Italic`/`BoldItalic` properties aligned with SkiaSharp 4.148. Shared styles must ignore public disposal and remain reference-stable; caller-created styles release their compatibility handle through inherited `SKObject` disposal. Construction and queries are `O(1)` and must not initialize WebGPU or inspect fonts.
* Preserve immutable `SKCubicResampler`/`SKSamplingOptions` value semantics and B/C coefficients from the compatibility API through `RenderCommand` and texture vertices. Descriptor construction is `O(1)` and must not sample, upload, compile shaders, or initialize WebGPU. Keep `default` anisotropy disabled at zero while the integer constructor clamps to at least one; filter, mipmap, cubic, and anisotropic constructors initialize only their native mode. Keep the Catmull-Rom fast path pixel-identical; do not collapse Mitchell and custom resamplers into a generic cubic mode.
* Preserve `SKTextBlob.CreatePathPositioned` tangent placement. Align glyph midpoint distances over the measured contour, use a half-open `[0, length)` range, shift sampled positions by half advance and normal offset, and retain rotation-scale matrices for non-warped text-on-path drawing. Complexity remains `O(G + P)` time and `O(G)` storage for G glyphs and path-measure setup P, with one retained glyph run and no readback.
* Route `SKCanvas.DrawText` point/scalar overloads into the canonical aligned text path and preserve legacy paint alignment plus native obsolescence. For text-on-path, `warpGlyphs=true` uses outline morphing and false uses tangent-positioned rotation-scale blobs. Wrappers stay `O(1)`; shaping/placement stay `O(G + P)`, retain compositor glyph batching, and must not reduce text quality or read pixels.
* Keep canvas clear/draw-color operations in device coordinates, inside active clip scopes, with exact requested blend mode and clamped `SKColorF`; they must ignore the current matrix. Build arcs as one bounded elliptical path, clamp complete turns to one oval, and close only center wedges. These operations stay `O(1)`, record at most one draw plus blend scopes, and never read pixels.
* Preserve `SKCubicResampler` as SkiaSharp's immutable two-float value contract with read-only B/C properties, native float equality and hashing, and exact Mitchell/Catmull-Rom constants. Retain both coefficients through `RenderCommand` and texture vertices. Keep the Catmull-Rom fast path pixel-identical; do not collapse Mitchell and custom resamplers into a generic cubic mode. Value construction, reads, comparison, and hashing remain allocation-free `O(1)` CPU work.
* Preserve `SKSamplingOptions` as SkiaSharp's immutable discriminated value contract. Default/filter/mipmap/cubic/anisotropic constructors, read-only properties, byte cubic discriminator, equality, and sequential hashing must remain native-compatible. Retain anisotropy through `RenderCommand`, compositor draw calls, and texture cache keys; clamp only the WebGPU sampler to `[1, 16]`, lazily cache by effective value, generate mipmaps, and dispose every cached sampler. Ordinary sampling must not allocate or enter the anisotropic cache. Value operations remain allocation-free `O(1)`; sampler lookup is amortized `O(1)` with at most 15 anisotropic entries.
* Preserve Skia pixel-center and alpha-representation semantics in `SKBitmap.Resize`. Keep nearest sampling free of source-sized temporary allocations and retain raw same-format texel copies; linear and cubic paths must remain stride-aware, and cubic must evaluate the requested B/C kernel rather than a generic bicubic approximation.
* Preserve the allocation-free double-queue dispatcher. Do not copy the pending queue into a new `List<Action>` per frame, execute newly posted work recursively in the same drain, or enqueue one delegate per benchmark element.
* Keep producer backpressure bounded. The LOL/s workload batches element mutations and limits pending work separately for VSync and uncapped runs so background production cannot starve rendering.
* Never infer PathAtlas demand from render-target width or height and never reserve or clear speculatively at frame advance. Only an actual shelf-allocation failure may trigger the single same-frame reset/recompile path; an impossible live set must fail explicitly after that retry.
* Never clear or repack the glyph atlas automatically while retained scenes or static buffers can reference its UVs. Capacity exhaustion must preserve every existing coordinate and generation, then render an uncached outline through the vector fallback. Tests must sample a color channel against the clear color; opaque target alpha cannot prove that a glyph was drawn.
* Glyph rasterization batches are nestable. Keep the normal compile path to one queue submission, but flush before the 256-byte-aligned uniform ring wraps; never reuse a uniform offset while an unsubmitted command encoder still references it. For G newly rasterized glyphs and ring capacity R, submission count is O(ceil(G/R)), raster work is O(total glyph pixels), and transient bind-group storage is O(min(G, R)).

### C. Required Performance and Quality Gates

Run the full renderer gates from the ProGPU repository root:

```bash
dotnet test src/ProGPU.Tests/ProGPU.Tests.csproj -c Release
dotnet test src/ProGPU.Tests.Headless/ProGPU.Tests.Headless.csproj -c Release
```

The current baseline is 1,756 renderer tests and 177 headless tests. Update these counts only when tests are intentionally added or removed.

The optional wavefront vector engine must remain lazy. Selecting the default atlas engine must not allocate wavefront pipelines, buffers, or a full-window intermediate texture. Wavefront rendering requires `Bgra8UnormStorage`, compiles text through shaped glyph positions, renders the ordinary scene into one reusable intermediate, applies compute work there, and resolves through a single-sample final pass. Preserve RGBA as the default headless-test contract and opt individual wavefront tests into BGRA targets.

Build once, then measure the exact final binaries:

```bash
dotnet build src/ProGPU.Samples/ProGPU.Samples.csproj -c Release

PROGPU_SAMPLE_BENCHMARK_PAGE='LOL/s Benchmark' \
PROGPU_SAMPLE_BENCHMARK_WARMUP_FRAMES=240 \
PROGPU_SAMPLE_BENCHMARK_MEASURE_FRAMES=480 \
PROGPU_SAMPLE_BENCHMARK_VSYNC=true \
dotnet run --project src/ProGPU.Samples/ProGPU.Samples.csproj -c Release --no-build

PROGPU_SAMPLE_BENCHMARK_PAGE='LOL/s Benchmark' \
PROGPU_SAMPLE_BENCHMARK_WARMUP_FRAMES=240 \
PROGPU_SAMPLE_BENCHMARK_MEASURE_FRAMES=480 \
PROGPU_SAMPLE_BENCHMARK_VSYNC=false \
dotnet run --project src/ProGPU.Samples/ProGPU.Samples.csproj -c Release --no-build
```

On the current 120 Hz reference machine, the protected targets are about 120 FPS and 12,000 LOL/s with VSync, and at least 36,000 LOL/s uncapped. The current measured values are 11,996 and 48,905 LOL/s. Compare on the same machine and window state; investigate a repeatable drop greater than 10% before merging.

Also run `Markdown Playground` and `DXF CAD Viewer` uncapped. Stable pages should miss once and then report nearly all `sceneCacheHits`; animated pages and LOL/s should report a useful miss reason rather than a false hit.

Validate Svg.Skia from its repository with `UseProGpuSkiaSharpShim=true`:

```bash
dotnet test tests/Svg.Skia.UnitTests/Svg.Skia.UnitTests.csproj -f net10.0 -c Release \
  -p:UseProGpuSkiaSharpShim=true -p:ProGpuSourceRoot=/absolute/path/to/ProGPU \
  --filter 'FullyQualifiedName~resvgTests'

dotnet test tests/Svg.Skia.UnitTests/Svg.Skia.UnitTests.csproj -f net10.0 -c Release --no-restore \
  -p:UseProGpuSkiaSharpShim=true -p:ProGpuSourceRoot=/absolute/path/to/ProGPU \
  --filter 'FullyQualifiedName!~W3CTestSuiteTests&FullyQualifiedName!~resvgTests'
```

The current shim baseline is 927 resvg passes with 37 explicit inventory skips and 1,147 remaining passes. The macOS CI W3C image lane has 47 established threshold differences, 483 passes, and 3 skips versus native SkiaSharp's 530 passes and 3 skips. Performance-only work must not add a fixture, increase an image error, or change a previously matching result. Compare the exact fixture/error list against the parent commit; intentional parity improvements should reduce the difference set and include their own image-focused review.

The `Svg.Skia parity` workflow is mandatory for changes under `src/SkiaSharp`, `ProGPU.Scene`, `ProGPU.Text`, or `ProGPU.Vector`. Keep its Svg.Skia commit pinned, run native and ProGPU checkouts separately, and preserve the TRX/PNG artifacts. `eng/progpu-verify-svg-skia-results.ps1` must compare the exact W3C fixture inventory in `eng/svg-skia-w3c-known-differences.txt`; never weaken it to count-only validation or make the image lane silently `continue-on-error`. A resolved difference requires image review and an intentional baseline/count update in the same commit, while any added difference blocks integration.

Record benchmark result lines and test totals in the commit or task summary. Do not claim a performance fix from subjective interaction alone.

---

## 3. Text Outline Diagnostic Tool (`TtfDiag`)

To debug text rendering quality, glyph geometry errors, or parsing inconsistencies, we maintain a generic command-line outline extractor tool under `tools/TtfDiag/`.

### How It Works
The tool loads any standard TrueType Font (`.ttf`) file, decodes its index tables, parses the outline contours using `TtfFont` parsing loops, and dumps the exact figure coordinates, segment types (Lines/Quadratic Beziers), endpoint coordinates, and control points in clean `CultureInfo.InvariantCulture` formatting.

### How to Run
Execute the tool from the root directory using the `dotnet` CLI:
```bash
# Run with macOS system Arial fallback to inspect specific glyphs (e.g., 'G' and 'g')
dotnet run --project tools/TtfDiag -- Arial Gg

# Run with an absolute path to a custom font and custom character sequence
dotnet run --project tools/TtfDiag -- /System/Library/Fonts/Supplemental/Georgia.ttf ABC
```

### Typical Output Format
Use the precise printed segments to check if coordinate coordinates contain anomalies, bounds issues, or flat segment joints:
```text
[TtfDiag] Loading font: /System/Library/Fonts/Supplemental/Arial.ttf
[TtfDiag] Units per Em: 2048, Total Glyphs: 3381

================================================================================
Glyph: 'G' | Unicode: U+0047 | Glyph Index: 42
================================================================================
Figures count: 1
  Figure 0: StartPoint = (844, 575), Closed = True, Filled = True
    Segments count: 29
      Segment 00: [Line]  -> End: (844, 747)
      Segment 03: [Quad]  -> End: (1170, 32.5) | Ctrl: (1322, 90)
```

Use this tool immediately whenever user reports "jagged glyph edges", "horizontal/vertical line artifacts crossing characters", or "weird transformations".
