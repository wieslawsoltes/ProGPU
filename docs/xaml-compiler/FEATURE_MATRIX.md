# XAML Compiler Feature and Maturity Matrix

Legend: `Done`, `Partial`, `Planned`, `Blocked`. Maturity columns are independent.

| Feature | Syntax | Schema | Semantic | Emission | Runtime | Evidence / next gate |
|---|---|---|---|---|---|---|
| Lossless XML token stream | Partial | n/a | n/a | n/a | n/a | Lexer reconstructs source; add malformed/fuzz corpus and green-tree reuse |
| Immutable public syntax nodes | Partial | n/a | n/a | n/a | n/a | Immutable green data/red views and non-mutating annotations exist; full node/token union and subtree reuse pending |
| Strict/recovery modes | Partial | n/a | n/a | n/a | n/a | Options exist; add explicit missing/skipped nodes and recovery tests |
| XAML infoset section 8 | Partial | n/a | n/a | n/a | n/a | Immutable green/red document/object/member/text model, namespace snapshots, markup-object conversion, strict/recovery, structural pass, lossless whitespace text, and explicit retrieved-object population for same-typed getter-only collection/dictionary property elements exist. Schema-aware binding implements default whitespace collapse/boundaries, inherited `xml:space`, significant collections, trim-surrounding child objects, and lexical-only-member suppression; East Asian linefeed and complete content-wrapper rules remain |
| Shared span-based markup-value tokenizer | Partial | n/a | n/a | n/a | n/a | Core parser, immutable versioned custom-language registry, trigger-indexed custom recognizers, deterministic priority/conflict diagnostics, arbitrary bounded nested delimiter maps, lossless protected commas/equals, infoset option propagation, and an allocation-free default delimiter tracker exist; add per-resolved-member generator selection, replace remaining eager strings, and add performance baselines |
| Attributed list-string grammar | n/a | Done | Done | Done | n/a | Avalonia's inherited class-only metadata retains exact source/reference evidence, defaults, custom longest-first separators, split flags, raw/trimmed spans, and invalid evidence. Bound items receive stable identities and use ordinary collection IR plus structured `Add` emission; add fuzz and throughput/allocation baselines |
| Compiled-binding annotation flow | n/a | Done | Done | Partial | n/a | Exact `DataType`, property/parameter scope inheritance, ancestor-items, and binding-assignment symbols survive source/reference compilation. Stable-ID contexts carry nearest-first bound sources and item values; structured assignment is profile-pluggable. A concrete Avalonia binding/path compiler and runtime service policy remain |
| Template/tooling annotations | n/a | Done | Done | Done | n/a | WinUI/WPF/Avalonia template parts, WinUI/WPF visual states and style-typed properties, and WPF attached-property browse rules retain exact source/reference symbols, inheritance, flags, and invalid evidence. Incremental generation reports malformed shapes. Template-instance conformance analyzers and editor completion consumers remain |
| Build/identity annotations | n/a | Done | Done | Done | n/a | Exact source/reference class/module/assembly compilation modes, WPF root namespace, MAUI file/resource identities, and WinUI bindable/full-provider markers are canonical Roslyn descriptors. Class/module/assembly precedence, `Skip`, relative `x:Class`, resource manifests, generator, and CLI consume one document snapshot; add real packaged MAUI/WPF profile integration |
| Save/editor/localization annotations | n/a | Done | Done | Done | n/a | Exact source/reference `DefaultValue`, designer serialization visibility/options, browsable/editor/design-time visibility, and WPF localizability evidence is canonical. Overridden-member inheritance and attached-getter metadata are supported; one neutral policy selects excluded/element/content/attribute form without changing load IR. Full graph serializer and designer/localization UI consumers remain |
| Whole-object serialization planning | n/a | Done | Done | Partial | n/a | Every bound document has a stable-ID immutable plan graph sharing `DependsOn` order with lowering. Plans retain omit/directive/attribute/element/content/constructor disposition, serializer/default/conditional/reset symbols, typed issues, and explicit constructor-mode selection. Structured Roslyn constructor/call syntax and policy-aware reverse editing consume it. Runtime object enumeration, default/condition evaluation, namespace allocation, and final lossless XAML writing remain |
| Attributed create-from-string | n/a | Done | Done | Partial | n/a | Windows metadata and registered equivalents resolve target-local or qualified public static one-string factories into the shared exact Roslyn create-from-string descriptor. Binding/IR retain raw text and callable evidence; structured invocation/cast syntax compiles. Additional framework catalogs and runtime metadata-provider publication remain |
| Roslyn-style markup-value syntax tree | Partial | n/a | n/a | n/a | n/a | Nested immutable model exists; lossless trivia/missing-token/rewriter/incremental reuse gates pending |
| Custom markup syntax plugins | Partial | n/a | Partial | Partial | n/a | Versioned context/trigger/precedence/associativity/capability/conflict registration, token integration, canonical AST projection, attribute infoset dispatch, inverse formatting, and round-trip generation exist. A separate immutable versioned Roslyn host accepts canonical markup-extension IR and emits user `ExpressionSyntax` with deterministic priority/coalescing and explicit conflict/failure diagnostics. Nested custom node/operator rules, semantic binders/validators, general IR transforms, broader emitters, package discovery, generator selection, and inverse document edits remain |
| Markup-extension syntax | Partial | Partial | Partial | Partial | Partial | Nested parser exists. Repeatable member bracket annotations retain exact source/reference evidence, validate complete pairs, and configure the shared parser without dialect forks; root and nested extensions are automatically reparsed per exact resolved member in binder, incremental-generator, and workspace paths. CLR extension identity combines registered base/interface/suffix evidence with a validated `ProvideValue` shape; declared return types are Roslyn symbols checked against target members. WinUI custom extensions reuse ordinary construction, emit a structured generic evaluation call carrying the logical resource URI, and receive target/root/read-only URI services at runtime. Full escapes/ABNF tests and non-WinUI invocation policies remain |
| XAML 2009 intrinsic schema | Partial | Partial | Partial | Partial | Planned | Canonical intrinsic directive/location data, scalar text value/pattern descriptors and validation, `x:Initialization`, `x:Null`, typed `x:Type`/`x:Static`, typed `x:Array`, `x:Arguments`, `x:FactoryMethod`, recursive `x:TypeArguments`, and same-scope `x:Reference` bind through explicit IR and structured emission; XML-Schema-regex matching, template/deferred namescope references, constructor-reference fixups, XData, generic constraint substitution, and remaining rules remain |
| Roslyn type provider | n/a | Partial | Partial | n/a | n/a | Types/properties/events/attached members/content, public constructor metadata, default-constructibility, static factory selection, assignability, generic-definition lookup, constructed symbol identity, basic constraint validation, exact type/member `TypeConverterAttribute` construction/callable shapes, independent type/member `ValueSerializerAttribute` save-path shapes, exact member `ConstructorArgumentAttribute` save-path projections, canonical trim/significant-collection/initialization/ambient Boolean descriptors, exact type/member `XamlDeferLoadAttribute` loader/content/callable shapes, repeatable exact markup-bracket pair descriptors, exact direct/attached namescope and language-directive alias shapes, exact interface/duck namescope ownership shapes, repeatable exact `DependsOn` dependency edges, typed Avalonia keyed/default markup-extension option descriptors and exact context-free/contextual selector shapes, repeatable typed content-wrapper descriptors, profile initialization-text types, and symbol-less pseudo members exist. Ambient evidence covers inherited types, ordinary members, attached getters, value-type propagation, stable deferred context snapshots, and neutral ambient-dictionary resource scopes. Deferred-loader evidence covers CLR/string identities, member-over-type propagation, node-stream binding, distinct IR, and structured load/save syntax. Markup-bracket evidence covers full constructor identity, source/reference symbols, conflict diagnostics, direct shared-parser policy, and exact-member semantic re-projection. Aliased-member evidence covers exact direct properties, attached owners/accessors, source/reference symbols, and XAML diagnostics while keeping namescope storage distinct from ownership. Namescope ownership retains exact interface/method symbols or explicitly enabled duck callables and drives nested semantic name/reference boundaries. Dependency evidence covers exact same-type property symbols and one stable topological order shared by binder, context graph, IR, and serialization plans. Avalonia option evidence covers official targets, typed values, priority, source/reference symbols, conflicting roles, exact selector methods, string/enum option types, service-provider types, candidates, malformed-shape diagnostics, and an optional structured profile emission seam whose generated conditional syntax compiles; concrete Avalonia evaluation, service-provider, and trimming policy remain. Serializer and constructor descriptors retain exact candidates/provenance/reference-symbol behavior and feed whole-object plans plus structured writer syntax without changing load semantics. Whitespace descriptors retain source/reference evidence and explicit inherited Boolean overrides; valid usable-during-initialization evidence drives explicit top-down IR/emission for ordinary created property, attached, collection, and dictionary children. Content wrappers retain exact constructor/content symbols and lower as ordinary typed objects after deterministic exact/most-specific selection. Substituted constraints, generalized non-TypeConverter create-from-string projections, runtime graph serialization/evaluation, complete runtime deferred readers/factories, ambient object-writer service stacks, and remaining projections remain |
| Attribute metadata engine | n/a | Partial | Partial | Partial | n/a | Provenance/priority-bearing rules retain canonical Roslyn `AttributeData`/`TypedConstant` values, including array-valued type evidence, per-rule repeatability, conflicts, aliases, ordering, and usage diagnostics. Cross-framework catalogs include deferred/root namespace, WinUI complete-metadata/bindability and markup-extension result types, MAUI compilation/file/resource identity and extension service declarations, .NET designer/save metadata, WPF serialization/localization metadata, and WPF object-writer interception attributes. Recognized assembly/module annotations, XML namespace mappings, type ancestry, and overridden-member ancestry are indexed; build controls and neutral save policies are consumed rather than merely cataloged. Composite projections, remaining framework inventories, and complete semantic consumption remain |
| Symbol-shape/duck typing | n/a | Partial | Partial | Partial | Partial | `Add`, dictionary, validated paired attached accessors, profile-authorized getter-only attached collections, extension base/interface/suffix plus exact callable/service descriptors, object-writer set callbacks, profile-declared add-child interfaces, exact property-identifier/setter evidence, exact-type implicit dictionary-key conventions, profile-owned resource-member roles, and provider-configurable designer `ShouldSerialize`/`Reset` methods exist. Providers explicitly declare contributed feature families, independent policies compose without replacement, and markup callable/service plus object-writer event-args validation stay scoped to their winning annotation/identity provider. All global scalar/keyed families retain canonical equal-priority conflict evidence and emit `PGXAML2049`; equivalent winners coalesce and lower-priority declarations remain recovery-only. WinUI, focused MAUI service, WPF interception, and user policies coexist in tests. Markup callables retain all candidates/provenance, prefer registered context overloads and unique most-specific service/interface types, and diagnose ambiguity. Required service symbols, available-profile validation, empty-provider permission, and mandatory declaration policy are canonical shape data. Set-value handlers retain exact nearest static callback/event-args symbols, classify direct accessibility versus typed-bridge need (including valid private callbacks), and lower through a profile-owned structured seam with unsupported-capability diagnostics. Designer save/reset methods retain exact nearest candidates, validate instance parameterless Boolean/void forms, reject `DefaultValue` mixing, and remain unexecuted typed policy callables. WinUI custom extension evaluation supplies typed target/root services. `VisualStateGroups`, `Style.TargetType`, and lexical/merged/theme/source resource vocabulary are profile data retained on canonical descriptors. Full wrapper/registration consistency and broader factory conventions remain |
| Framework profile contract | n/a | Partial | Partial | Partial | n/a | Contract v2 has validated factories, deterministic host registry, capability flags, synthetic vocabulary, typed resource-expression lowering, optional structured compiled-resource publication, and optional structured property-system resource assignment; external package discovery, dependency ordering, and conflict diagnostics remain |
| Bound tree and validation rules | n/a | Partial | Partial | n/a | n/a | Canonical/error references, assignability, aliases, synthetic named arguments, intrinsic type/static/array/text validity, enum text, converter shapes, constructor/factory matching, intrinsic/profile directive locations, member multiplicity, local lexical resource graphs, and deferred-template namescope isolation have diagnostics; complete section 5/6 catalog, projected namescope owners, and remaining cross-file scopes remain |
| Construction IR | n/a | n/a | Partial | Partial | n/a | Explicit create/retrieve/initialize/array/extension/set/attach/add/dictionary/event/intrinsic/directive, typed resource-reference, deferred-content operations, and bottom-up/top-down object initialization modes exist; member dependencies receive stable topological ordering. Valid usable-during-initialization children are published before population through structured property, attached, collection, and dictionary operations. Deferred factory emission and operation-valued markup-extension assignment are implemented; broader bindings, explicit namescope operations, and optimization passes remain |
| WinUI object construction | Done | Partial | Partial | Partial | Partial | Representative controls plus core resource, grid, storyboard/key-frame, visual-state, transition, template, items, list/grid-view presenter, scroll presenter, and command-bar public shapes compile in the generated-C# Roslyn harness. Canonical `Control.BackgroundSizing`, double-valued `UIElement.Opacity`, font-weight conversion, animation/key-frame dependency-property identities, and public AppBar/button/toggle/calendar/command/split-view template-settings types now support unchanged Fluent templates and typed templated-parent paths. Split settings retain calculated open/compact scalar and grid lengths. WinUI `ItemsPanel`/`ItemTemplate` retain their template types while ProGPU's live-panel/container factory hooks are separate extensions; full object model and runtime behavior remain |
| Names and events | Done | Partial | Partial | Partial | Planned | Fields/event hookup, duplicate/missing reference validation, typed `x:Reference`, deferred forward property/collection assignments, and independent deferred-template duplicate-name scopes exist; enter/exit namescope IR, projected namescope owners, template-local reference emission, and event signature validation remain |
| Attached properties | Done | Partial | Partial | Partial | Partial | Public static getter/setter pairs require matching value types, target assignability, no ref parameters, and unique overloads; framework property-system projection pending |
| Resources and theme dictionaries | Done | Partial | Partial | Partial | Partial | Classless dictionaries emit typed Roslyn `Build`/`Populate` factories and per-artifact typed URI registration. Logical identities preserve scheme, authority/component assembly, and path for `ms-appx`, `avares`, WPF component/pack, root-relative, and relative forms; qualified providers never leak through suffix fallback. Immutable text/type/static key descriptors drive duplicate checks, lexical graphs, nested references, symbol-valued key diagnostics, structured key emission, and object-key runtime markers. Conditional dictionaries are sibling typed-key partitions: duplicate keys are isolated per variant; static and dynamic lookup retain all nearest active-theme variants, with load-time snapshot versus theme-change re-evaluation represented by distinct reference roles; partition-local references do not leak across siblings; single keyed entries are not mistaken for retrieved property objects; and provider-root/transitive per-variant `Source` imports preserve partition identity and provenance. Partition identity participates in raw/semantic manifests, external slices, fingerprints, generated Roslyn dictionary insertion, and incremental invalidation. WinUI `Style.TargetType` supplies a validated implicit type key. Version/public-key component qualifiers, site-of-origin policy, generalized conditional predicates, and template factories remain |
| `StaticResource` | Done | Partial | Partial | Partial | Partial | Synthetic schema, local lexical resolution, forward-reference diagnostics, scope-owned cross-file imports, nearest-scope and reverse-merged provider resolution, provider provenance, source-located import diagnostics, typed resource IR, structured nullable-aware lookup, and reflection-free WinUI resolution exist; application/system precedence and deferred/template scopes remain |
| `ThemeResource` | Done | Partial | Partial | Partial | Partial | Generated references retain the typed XAML-unit lookup root; compile-time graphs retain every applicable local or external theme variant with typed partition and provider identity; dependency-property storage resolves local/ancestor/merged/theme/application scopes, re-evaluates on theme and resource changes, preserves roots through brush adapters, and rejects alias cycles. One invariant-culture runtime converter serves theme resources, setters, visual states, and template bindings, including nullable numeric targets during initial resolution and re-evaluation. Dictionaries publish monotonic cycle-safe generations and transactional `Source` replacement. Validated property-system symbols drive structured `SetValue` assignment for non-brush typed CLR wrappers. Variant completeness diagnostics, deferred/template scopes, and non-WinUI partition policies remain |
| `Binding` | Done | Partial | Partial | Partial | Partial | Synthetic extension identity and canonical options bind normally. Ordinary binding has distinct bound/IR nodes retaining lossless path syntax, neutral source kind, optional source type, and exact readable CLR member or constant mutable indexer/source/value/write Roslyn symbols. Lexical `x:DataType`, explicit text/value, exact `x:Static`, exact local/external resource-graph values, `RelativeSource Self`, provable same-namescope `ElementName`, and profile-resolved `RelativeSource TemplatedParent` sources publish only their own accessors. Immutable post-bind source passes treat forward/backward names equally, isolate deferred scopes, accept exact deferred context through bound/IR profile contracts, and enrich resource sources only when all reachable definitions, aliases, variants, or semantic-manifest candidates share one Roslyn type; compiler core contains no WinUI template, target-member, mode, or resource-runtime names. Unresolved runtime selectors suppress lexical evidence. Integer `IList<T>` and string `IDictionary<string,T>` steps lower as structured closed-generic interface-cast delegates plus immutable runtime path segments through dedicated neutral profile contracts. WinUI generated paths use `SetBindingWithPath` and are consumed without runtime tokenization; dependency-property, member, collection, and following-item notifications rewire normally, including TwoWay leaf writes. Class-backed units begin one ordinary-binding lifetime before construction and attach it after content and generated names commit; deferred calls retain context, lookup root, and an independent per-materialization owner. Generated module initialization is deterministic and deduplicated with `PGXAML2132`/`PGXAML3047`/`PGXAML3048`/`PGXAML3049` failure paths. The reflection-free runtime also supports runtime-only dotted-member fallback, data context, explicit template-context fallback, source/self/templated-parent/element-name roots, one-time/one-way/two-way, property-changed/explicit/lost-focus updates, converters, fallback/target-null, clearing, deferred activation, and exact root ownership. Ordinary and compiled lifetimes coexist on one root and dispose independently from sibling roots over the same item. The unchanged Fluent corpus compiles named and templated-parent paths, and the generated sample executes lexical/static/resource/self/indexed/named/templated typed paths without handwritten registration. Collection views/current item, multi-argument/typed indexers, validation, async bindings, richer path grammar, diagnostics/source maps, non-WinUI runtimes, and full WinUI parity remain |
| `x:Bind` | Done | Partial | Partial | Partial | Planned | Distinct expression role, bound node, and IR node retain the canonical root or scoped `x:DataType` Roslyn type plus exact property/field/indexer/cast/attached/function source/result/write symbols. The shared lossless grammar supports dotted members, constant integer/string indexers, grouped C#-style and pathless casts, legacy qualified members, and terminal instance/static functions with typed path/string/number/language-Boolean arguments. Roslyn conversions and schema member shapes disambiguate casts from attached access and select exact function methods/parameters. WinUI emits typed static Roslyn delegates, cast/function segments, mutable collection interface access, attached getter/setter calls, and exact dependency-property identifiers into a reflection-free runtime with OneTime default, inherited `x:DefaultBindMode`, OneWay/TwoWay, nested and function-argument rewiring, BindBack, converter/fallback/null/update-trigger policy, and generated page/control plus per-materialized-template lifecycles. Deferred factories begin and explicitly pass one controller per invocation, attach it after root commit, and dispose it on root/unloaded/subtree replacement without affecting another root over the same item. Event binding resolves a unique compatible method and emits an event-time owner-path lambda. Dedicated samples cover page properties, events, indexed collections/dictionaries, casts, attached members, functions, object replacement, lifecycle controls, and two independently tracked typed template roots over one item. Direct, incremental-generator, runtime, and sample gates pass. Richer diagnostics/source maps, nested function expressions/null propagation, and other framework profiles remain |
| Templates and visual states | Done | Partial | Partial | Partial | Partial | Parser-owned `FrameworkTemplate.Template` is a symbol-less deferred member; `SetDeferredContent`, semantic namescope isolation, core storyboard/key-frame/theme-animation dependency properties, transitions, and getter-only `VisualStateGroups` bind. The core emits isolated structured-Roslyn factory lambdas through a profile-owned typed runtime publication contract, and WinUI creates a fresh tree per invocation. A root owns a composite ordered `IXamlTemplateLifetime` set; ordinary and compiled binding groups initialize after construction and dispose on release, `Unloaded`, or subtree replacement without affecting sibling materializations. Runtime state lookup traverses the generated template root, transactionally applies/restores setters and final values from object, double, double-key-frame, and color animations through registered dependency properties, and preserves raw theme markers. Generated Button and CheckBox interaction states execute through normal control changes. Explicit enter/exit namescope objects, duration/transition clocks, nested property paths, named non-visual targets, specialized theme animations, in-place materialized-instance hot-reload patching, and full template behavior remain |
| WinUI Fluent `generic.xaml` unchanged | Done | Done | Done | Done | Partial | The unchanged 2,045,878-byte/23,820-line upstream file passes strict binding and resource analysis, emits structured Roslyn, compiles with zero warnings/errors, registers its logical URI, and constructs Default/Light/HighContrast plus root styles. Partition-local lookup roots and retained Light↔HighContrast switching are covered. Every provable ordinary templated-parent path now binds against the exact `ControlTemplate.TargetType`; this exposed and added the public AppBar/button/toggle/calendar/command/split-view template-settings contracts instead of suppressing typed diagnostics. Generated implicit Button, CheckBox, and ComboBox styles instantiate their template trees; Button/CheckBox execute pointer/checked/enabled final-value states, while ComboBox executes templated-parent placeholder and two-way editable-text bindings and detaches replaced-template subscriptions. Canonical ComboBox template settings, split animations, TextBox/TextBlock dependency properties, and automation attached-property template bindings are validated. `ProGPU.WinUI.Themes.Fluent` links rather than copies the source, packs the unchanged payload, and the sample applies it. A native OS provider, live mutation of non-DP system-color receivers, duration-based and specialized animation behavior, broader control-state coverage, hot reload, accessibility behavior, and image baselines remain |
| Incremental source generator | Done | Partial | Partial | Partial | n/a | Parse/infoset/raw-manifest stages remain compilation-independent; a separate compilation/profile-dependent semantic-manifest stage publishes canonical framework identities. The project index supplies value-equatable transitive slices; tests prove unrelated edits reuse semantic manifests and both `using:` and presentation/implicit keys survive provider boundaries. Framework analyzer facades can explicitly supply an immutable profile registry reused by semantic and emission stages; deterministic catalogs, custom selection, generated compilation, and package-specific unknown-profile diagnostics are covered without scanning. Automatic multi-package discovery/arbitration and performance baselines remain |
| NuGet/MSBuild integration | n/a | n/a | n/a | Partial | n/a | The exhaustive shipping manifest includes core, Roslyn, generator, Workspaces, CLI, Fluent, and runtime packages. Analyzer binaries and portable symbols are co-located, dependency DLLs are filtered from Roslyn analyzer inputs, and transitive facade declarations select exactly one analyzer for `ProGpuXamlFramework` before compilation. The isolated private-cache/local-feed consumer builds, runs, and inspects generated `x:Bind`, event, and typed resource-source binding output, then proves missing and duplicate facade declarations fail deterministically. The gate also installs the produced CLI into an isolated tool path and parses consumer XAML. Deterministic artifacts across checkout roots and published-feed installation remain |
| CLI parse/corpus | Done | n/a | n/a | n/a | n/a | Unchanged Fluent corpus parses |
| CLI project/workspace compile | n/a | Partial | Partial | Partial | n/a | `MSBuildWorkspace`, metadata-backed dependency loading, syntax/infoset/bound/resource-graph/IR inspection, JSON diagnostics, JSON manifest, and transactional writes exist. Analyzer-only project dependencies no longer become spurious application-reference warnings; the CLI tool now participates in the verified shipping manifest. Full generator-equivalence testing remains pending |
| XAML-only hot reload | n/a | n/a | Partial | Partial | Partial | Writable content roots use commit-before-field-publish and state restoration; collection-root replacement and XAML delta host remain pending |
| Avalonia profile | Done for XML | Planned | Planned | Planned | Planned | Separate profile package |
| WPF profile | Done for XML | Planned | Planned | Planned | Planned | Separate profile package |
| MAUI profile | Done for XML | Partial | Partial | Planned | Planned | Neutral catalog and focused shape provider validate generic/non-generic extension interfaces, exact `ProvideValue(IServiceProvider)`, required-service arrays, available services, and require-or-empty policy. A distributable MAUI profile/runtime package remains |
| User plugins | Done for syntax analyzers | Planned | Partial | Partial | n/a | Composition includes the versioned custom markup-language registry plus an immutable Roslyn host. Bound validators compose after resource enrichment; ordered construction transforms preserve bound/resource/root identity and pass a bounded iterative audit; markup-expression handlers consume canonical IR and return structured syntax. Framework profiles may contribute a prevalidated host without reflection, merged with caller extensions under one global order, strictest transform bound, and XAML-located duplicate rejection. Registration snapshots, priority/order, exact diagnostics, strict behavior, IR rewriting, structural coalescing, conflicts, invalid results, exceptions, fallback, cancellation, and generated-tree compilation are tested. Schema/binder transforms, specialized phase factories, statement/declaration/shared emitters, package discovery/dependencies, source-generator/MSBuild selection, and a public compatibility kit remain |
| Roslyn `SourceText`/span/location/diagnostics | Done | n/a | n/a | n/a | n/a | Custom parallel text/span abstractions removed |
| Green/red Roslyn-compatible XAML tree | Partial | n/a | n/a | n/a | n/a | Immutable green data/red views exist; incremental subtree reuse and full node/token union pending |
| XAML syntax editor / text changes | Partial | n/a | n/a | n/a | n/a | Batched replace/insert/remove and AdditionalDocument solution service exist; structural node factories/rewriter pending |
| Rule-based XAML formatter | Partial | n/a | n/a | n/a | n/a | Shared markup formatter emits idempotent `TextChange` values; complete document/profile rule chain pending |
| Bidirectional XAML/C# projection | Partial | n/a | Partial | Partial | Planned | Literal expressions carry checksum/stable-node/member/kind/span annotations; conservative reverse property-literal edits verify Roslyn semantic identity, escape XML, and reject stale/missing/ambiguous/overlapping edits transactionally; broader inverse registry pending |
| Roslyn C# `SyntaxFactory` emitter | n/a | n/a | Partial | Partial | n/a | Emits only from construction IR using structured annotated nodes; harness compiles output and source audit prohibits parse-back APIs; full IR operation set pending |
| XAML playground | Partial | n/a | Planned | Planned | Planned | Live lossless parser plus infoset object/member/text metrics are in samples; bound/IR/generated/live-preview panes pending |
| Generated XAML sample pages | Done | Partial | Partial | Partial | Partial | Six source-generated pages cover compiler introduction, layout/conversion, markup extensions, compiled property/event/indexer/function/template bindings, ordinary-plus-compiled per-instance template lifetimes, and local/static/dynamic resources plus styles. The separate playground exposes live syntax/infoset metrics; bound/IR/generated/live-preview panes remain |

## Diagnostic ranges

| Range | Ownership |
|---|---|
| `PGXAML1xxx` | source, XML lexer/parser, XML-to-XAML conversion |
| `PGXAML2xxx` | schema, binding, validation, markup semantics |
| `PGXAML3xxx` | lowering and code generation |
| `PGXAML4xxx` | resources, templates, bindings, cross-file graphs |
| `PGXAML5xxx` | framework profile compatibility |
| `PGXAML8xxx` | build, packaging, generator host, CLI/workspaces |
| `PGXAML9xxx` | internal invariant failure (always a compiler defect) |
