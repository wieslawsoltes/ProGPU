# ProGPU.CAD Feature Matrix

Status values: `planned`, `foundation`, `in progress`, `verified`.

| Area | Required result | Status | Verification gate |
|---|---|---:|---|
| Dependency | ACadSharp fork pinned as a submodule | foundation | clean recursive checkout and build |
| IO | DXF/DWG stream open with progress, diagnostics, cancellation, and initial byte/entity limits | foundation | version/corrupt/fuzz fixtures and path host adapter |
| Save | caller-owned stream save with version/certification gates and deferred destination commit | foundation | atomic path replacement and advertised-version semantic round trips |
| Model | ACadSharp `CadDocument` authority with locked callbacks, atomic generation capture, and content/saved generations | in progress | broader edit transaction and concurrency tests |
| Editing | typed atomic commands, undo/redo, dirty generations | in progress | bounded generation-synchronized history plus translate/pivoted axis-angle rotate/pivoted uniform scale; entity duplicate, visibility, layer, linetype, linetype-scale, lineweight, color, and transparency commands; layer add, visibility, plot eligibility, color, lineweight, and linetype commands; single model-space add/remove; caller-buffered generation-tagged BVH candidates and deterministic semantic root-handle deduplication; exact allocation-free point hits for lines, conformal circles/arcs, conformal signed-bulge and straight 2D polylines, 3D polylines, positive-weight open/closed/periodic rational splines, filled TrueType TEXT/MTEXT outlines and retained decorations/masks/stack separators, stroked analytic SHX TEXT/decorations, filled SOLIDs, and visible 3DFACE edges; exact world-space box Window/Crossing tests cover the same geometry families; bounded degree-29 Bernstein root isolation returns typed unsupported results for numerically unresolved clusters instead of tessellation guesses; one caller-buffered query composes broad phase, exact tests, unsupported counts, and semantic handles; and the shared shell provides finite-step multi-selection WCS translation plus selection-centered WCS-Z rotation and uniform scaling with one generation/rebuild per transform/Undo/Redo; color/bitmap glyph picking, arbitrary-camera projected selection, the dashed-linetype visible-vector Window exception, typed UCS/arbitrary base-point/reference input, atomic multi-delete, draw-order resolution, populated-layer removal, broader property/tool families, and fuzz tests remain |
| 2D | all standard entities, blocks, XRefs, hatches, dimensions, images | in progress | line/circle/arc/ellipse/NURBS/lightweight and legacy 2D polyline/SOLID/3DFACE/TrueType TEXT plus formatted TrueType MTEXT and bounded nested static INSERT/MINSERT snapshot and top projection implemented; dynamic blocks/XRefs/attributes, remaining families, and multi-DPI/zoom image tests remain |
| 3D | meshes, surfaces, solids, ACIS SAT/SAB, orbit/depth | in progress | immutable 3DFACE and legacy 3D-polyline records with exact 3D bounds implemented; shaded/depth compiler and matched managed/native 3D fixtures remain |
| Text | complete ProGPU Unicode/OpenType text stack | in progress | single-line TrueType TEXT uses typed font resolution, retained ProGPU shaping/fallback runs, two-point Align/Fit scaling, metric-driven decorations, conservative affine bounds, and vector glyph-run recording; clean-room TrueType MTEXT now retains scoped font/height/width/tracking/oblique/alignment/color/decoration state, paragraph-wide bidi/fallback shaping, cluster-safe wrapping, AtLeast/Exact spacing, attachment, forced/static/dynamic/reverse-flow columns, gutter-excluding masks/frames, semantic fraction/tolerance stacks, exact affine selection, native-picture replay, and print-plan reuse. SHX/Big Font and vertical MTEXT, fields, typed paragraph indentation/tab stops, CAD font search/substitution UI, and broader bidi/color/variable-font differentials remain |
| SHX | regular, Unicode, Big Font, shapes, substitution | in progress | bounded immutable standard AutoCAD-86 parser, commands 0–14 analytic interpreter, reusable catalog/FMP policy, ordered bounded desktop discovery, standard control/placement layout, retained horizontal TEXT with coalesced metric-driven decoration strokes, dual-orientation default-insertion vertical TEXT, shared path recording, and pinned compiled-fixture regression implemented; compiled Unicode/Big Font containers, non-default/decorated vertical placement, broader conformance corpus, and fuzzing remain |
| Rendering | retained analytic GPU scene with incremental chunks | in progress | immutable typed streams, BVH, WCS rebase, affine analytic static-block/array expansion, retained shaped TEXT and colored/formatted MTEXT glyph ranges, bounded model-space simple and complex-linetype expansion with definition-shared TrueType/standard-SHX text and SHX-shape payloads plus exact open, closed, and compact/expanded-periodic rational-SPLINE traversal through degree ten, first retained scene, plottable-layer filtering, JSON snapshot/scene/print-plan CPU baseline harness, and transactional whole-generation picture replacement after shell edits implemented; shared block fragments/instances, incremental chunks, stale background-publication gates, and full GPU replay metrics remain |
| Camera | uniform-only pan/zoom/orbit after compilation | in progress | first recorded plan scene is camera-independent; shared typed WCS-rebased plan viewport provides matching camera, world-to-screen, screen-to-world, full-depth selection-column mapping, and O(1) pan compensation that preserves WCS projection across snapshot rebase changes; orbit and entity-count-independent CPU counters remain |
| Line quality | model cosmetic and paper/plot physical lineweight | in progress | fixed device-space/hairline recording plus output-DPI physical print-plan lineweight; packed referenced LTYPE definitions; checked global×entity scale; A-aligned dash/gap/dot lowering for line/circle/arc/ellipse, open/closed/periodic rational SPLINEs through degree ten, 2D/3D polylines, and visible 3DFACE edges; definition-shared TrueType/standard-SHX text and SHX-shape complex elements with persisted relative/absolute rotation, S-independent scaled offsets, tangent-aware placements, explicit substitution diagnostics, and a transactional placement budget; per-entity 2D PLINEGEN; analytic curve splitting; transactional figure/descriptor/source/arc-map limits; and managed/native picture compilation implemented. LIN-only upright reconstruction, decorated/nonzero-advance complex descriptors, Unicode/Big Font resources, dot-first/closed-pattern AutoCAD differential confirmation, annotation/paper-space scale policy, plot-style overrides, and multi-backend physical-output comparisons remain |
| Native parity | equivalent managed/native behavior and quality | planned | differential scene/pixel/performance suite |
| Desktop app | standalone ProGPU.CAD desktop viewer/editor | in progress | shared retained viewer with click, Window/Crossing drag selection, semantic-handle state, complete semantic-root overlays, middle/right pan, finite WCS-step `±X`/`±Y` Move, selection-centered `↺`/`↻` Rotate and `×`/`÷` uniform Scale, Undo/Redo enablement, native open/save pickers, drawing-local plus explicitly ordered support-path SHX discovery, dynamic toolbar/status shell, and a real `floorplan.dxf` open/render smoke pass; broader property/edit tools and certified output remain |
| Browser app | browser/AOT viewer/editor with streamed IO | in progress | the same shared retained viewer, click/drag selection, overlay, pan, Move/Rotate/Scale/Undo/Redo shell, and picker/download workflow compile through native-linked Wasm, and Release AOT publish passes; interactive browser IO and performance smoke remain |
| Printing | layout/viewports/plot styles/preview/output | foundation | bounded generation-tagged extraction of layouts and standalone named page setups plus detached printer/media, millimeter paper/margins/origin, unit, rotation, area/DCS-window, scale, lineweight/style, and shade policy; exact model-space Extents wireframe lowering including retained formatted MTEXT to paper pixels, all four page rotations with oriented asymmetric margins and rotated-origin offsets, fit/current custom scale, centered or printable-relative offset placement, output-DPI fixed lineweight, one clipped retained page replay, explicit transparent-style rejection while the dependency lacks Plot Transparency, ownership/cancellation/unsupported-policy tests, and JSON p50/p95/p99 baseline implemented; layout viewports, DCS/named-view/UCS lowering, pixel media scale, CTB/STB, hidden/shaded/transparency policy, preview UI, printer/PDF/SVG/raster adapters, multi-page collation, and matched output comparisons remain |
| Export | vector and raster output with text/line fidelity | planned | round-trip and image-quality baselines |

This matrix is a coverage index, not a completion claim. A row becomes
`verified` only when its stated gate covers the full required result.
